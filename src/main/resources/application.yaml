server:
  port: 9094
spring:
  application:
    name: ORDER-SERVICE
  profiles:
    active: dev
  data:
    mongodb:
      uri: mongodb://localhost:27017/orderdb
      host: localhost
      port: 27017
      database: orderdb
      repositories:
        enabled: true